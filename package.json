{
  "name": "mhra-server",
  "version": "1.0.0",
  "description": "A simple json-server for MHRA project",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8080"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
